# Retained Nox Causal Sources

- These packages are retained research/source modules during the Hermes foundation migration; they are not production workspaces or an alternate model loop.
- Do not connect `cortex-pi`, `runtime`, `interface-rpc`, or `store-sqlite` to the production Desktop unless the active plan assigns a bounded seam and its graph test is updated deliberately.
- Future Journal integration may observe Hermes outcomes and own Nox constitutional State; it must not duplicate Hermes sessions, messages, tool results, or model-loop authority.
- Keep `testkit` unreachable from every production entrypoint.
- Preserve deterministic clocks, IDs, schema validation, append-only causal records, and independent auditability when causal components are reused.
