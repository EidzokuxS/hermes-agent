# Pi CortexPort acceptance evidence

Captured at `2026-07-12T09:14:15.655Z` for Task 4 of the accepted first-causal-loop plan.

## Authority fence

`PiCortex` invokes pinned `@earendil-works/pi-agent-core@0.80.6` through the low-level `runAgentLoop` with a fresh `AgentContext` for every Act. `shouldStopAfterTurn` forces one provider transition even when tool validation fails. Steering/follow-up callbacks, Agent session state, Pi persistence, and Pi queues are absent.

The context exposes exactly one tool name: `propose_act`. Its parameters are the JSON Schema generated by the shared strict `ActProposal` schema; its result has `terminate: true`. There are no State-changing, filesystem, shell, memory, network, session, or generic tool surfaces. Runtime still treats the returned value as an `ActProposalResult`, never as a commit.

The direct production dependency report is:

```text
@earendil-works/pi-agent-core@0.80.6
@earendil-works/pi-ai@0.80.6
@nox/protocol@0.0.0 -> zod@4.4.3
```

Static imports contain only those three package boundaries and local Nox modules.

## Outcome matrix

| Provider/tool behavior | Typed outcome | Provider calls |
| --- | --- | ---: |
| one valid `propose_act` | `proposed` | 1 |
| visible text, no tool | `plain-text` | 1 |
| two valid terminal calls | `duplicate-proposal` | 1 |
| forbidden/malformed Effect arguments | `schema-invalid` | 1 |
| tool call with `stopReason=length` | `truncated` | 1 |
| provider `stopReason=error` | `provider-error` | 1 |
| signal aborted before invocation | `aborted` | 0 |

Two successive Acts through the same `PiCortex` instance each presented exactly one fresh user message to Pi; no prior assistant/tool transcript crossed the boundary. Model identity drift between CortexInput and configured Pi model was rejected before invocation.

## Bounds and artifacts

- Requested output tokens are capped by the selected Pi model limit.
- Act timeout and the composed AbortSignal are passed to Pi; hidden provider retries are disabled with `maxRetries=0`.
- Operational artifacts contain canonical API input, model identity, visible text, tool calls, usage, stop reason, error message, diagnostics, and an accepted tool proposal when available.
- A faux response containing an explicit thinking block proved that its content does not appear in the artifact. No credentials are part of the artifact contract.

## Verification

- `npm run test --workspace @nox/cortex-pi`: 1 file, 9 tests passed.
- `npm run typecheck --workspace @nox/cortex-pi`: passed.
- `npm run build --workspace @nox/cortex-pi`: passed with declarations.
- `tests/live/real-pi-first-loop.ts`: standalone strict TypeScript check passed and remains an explicit real-provider acceptance entrypoint for Task 11.
