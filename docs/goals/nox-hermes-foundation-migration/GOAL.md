# Goal: Переезд Nox на Hermes Foundation

Use Krypton Execution to execute `docs/goals/nox-hermes-foundation-migration/PLAN.md` after its exact hash is explicitly accepted.

Core rules:

- Treat the full pinned Hermes Agent tree as the product foundation, not as a UI donor slice.
- Preserve `NOX-CONVERGENCE.md` as read-only conceptual truth and require a separately accepted `identity/NOX.md` before integration.
- Keep Hermes `state.db` authoritative for operational session/message/tool facts during this goal.
- Let Nox Journal own only Nox causal/provenance records and constitutional State; label observed Hermes outcomes as observed, not authored.
- Keep one production Desktop and one production model/tool loop. Do not run the old Node Nox runtime or a shadow cortex behind Hermes Desktop.
- Put canonical Nox identity in a stable, versioned prompt tier. Keep profile `SOUL.md`, memory and project context additive.
- Preserve Hermes prompt caching, tests, UX and full feature baseline unless a documented Nox invariant requires a bounded change.
- Defer visible branding and data-path changes until the identity and causal seams pass.
- Delete or demote every displaced custom Desktop/runtime path at cutover; do not retain compatibility routes without a verified contract.
- Capture target-perspective evidence from a packaged Desktop and say `implemented but unproven` if Nox identity, restart correlation, parity or displaced-path evidence is missing.
- Keep `NOX-RETHINK.md` and `REFERENCE ONLY/**` closed.

